# Generated Outputs

This directory is reserved for analysis outputs created by the Stata pipeline.

Expected subfolders:

- `output/tables/`: LaTeX tables from `esttab`
- `output/figures/`: generated figures, if any
- `output/logs/`: Stata logs

These files are reproducible from the raw data and the do-files, so they are intentionally not tracked in Git.
